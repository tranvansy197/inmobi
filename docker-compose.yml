services:
  redis:
    image: redis:latest
    container_name: redis
    ports:
      - "6379:6379"
    command: redis-server --requirepass Password#1
    volumes:
      - redis-data:/data
    restart: unless-stopped

volumes:
  redis-data:
